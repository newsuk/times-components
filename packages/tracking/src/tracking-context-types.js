// @flow
import PropTypes from "prop-types";

// @TODO: replace this PropType type with the flow one (TrackingContextTypes) below
export default {
  tracking: PropTypes.shape({
    analytics: PropTypes.func
  })
};
