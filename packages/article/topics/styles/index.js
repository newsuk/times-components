import sharedStyles from "./shared";

const topicStyles = {
  ...sharedStyles,
  topicsContainer: {
    ...sharedStyles.topicsContainer,
    marginLeft: 10,
    marginRight: 10
  }
};

export default topicStyles;
