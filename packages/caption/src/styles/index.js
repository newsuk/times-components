import sharedStyles from "./shared";

const styles = {
  ...sharedStyles,
  credits: {
    ...sharedStyles.credits,
    lineHeight: 17,
    marginBottom: 0
  },
  text: {
    ...sharedStyles.text,
    lineHeight: 19,
    marginBottom: 4
  }
};

export default styles;
