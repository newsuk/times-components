import sharedStyles from "./shared";

const styles = {
  ...sharedStyles,
  credits: {
    ...sharedStyles.credits,
    lineHeight: 17
  },
  text: {
    ...sharedStyles.text,
    lineHeight: 17
  }
};

export default styles;
