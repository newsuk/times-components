import { spacing } from "@times-components/styleguide";

const styles = {
  column: {
    width: "50%"
  },
  container: {
    flexDirection: "row",
    paddingHorizontal: spacing(4)
  }
};

export default styles;
