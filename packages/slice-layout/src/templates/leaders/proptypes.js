import PropTypes from "prop-types";

const propTypes = {
  renderLeader1: PropTypes.node.isRequired,
  renderLeader2: PropTypes.node.isRequired,
  renderLeader3: PropTypes.node.isRequired
};

export default propTypes;
