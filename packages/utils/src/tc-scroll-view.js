import styled from "styled-components";
// not sure how else to put the default styles, this the default style for a React Native <Text/> component

const TcScrollView = styled.div`
  height: 500px;
  overflow-y: scroll;
  white-space: nowrap;
`;

export default TcScrollView;
