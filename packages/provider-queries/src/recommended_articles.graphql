query RecommendedArticles(
  $publisher: Publisher!,
  $recomArgs: RecommendationsParameters!
) {
  recommendations(
    publisher: $publisher,
    recomArgs: $recomArgs
  ) {
    articles {
      id
      headline
      label
      publishedDateTime
      updatedDateTime
      url
    }
  }
} 