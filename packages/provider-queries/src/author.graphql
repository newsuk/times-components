query AuthorQuery($slug: Slug!) {
  author(slug: $slug) {
    articles {
      count
    }
    biography
    hasLeadAssets
    image
    jobTitle
    name
    twitter
    metaDescription
    contractualTitle
  }
}
