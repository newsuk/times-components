import { Meta, Story, Props } from '@storybook/addon-docs'
import { TCThemeProvider, themes,GridOverlay } from '../../utils/index.tsx';
import { LeadStory1 } from "./";
import { leadArticle,articles,smallArticles,groupedArticles,singleArticle,articlesWithListItems } from '../fixtures/lead-story.json';
import { action } from '@storybook/addon-actions';

<Meta
  title="Newskit Slices/Lead Story 1"
  component={LeadStory1}
/>

# LeadStory1 component
The `LeadStory1` component is used to display the main story, supplementary articles and comment quotes.
It is comprised of the `leadArticle`, `ArticleListItem`, `smallArticles`, `groupedArticles`, `singleArticle`, and `articlesWithListItems` slice components.

This takes in a series of props, as below, to display the part of the slices.

## Props
<Props of={LeadStory1} />

## Code Example
`<LeadStory1 {...{ leadArticle, comments, smallArticles,groupedArticles,singleArticle,articlesWithListItems }} />`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const LeadStory1Story = ({ leadArticle,articles,theme,smallArticles,groupedArticles,singleArticle,articlesWithListItems,gridOverlay, clickHandler, expirableFlag, contentType, articleLabel}) => (
  <TCThemeProvider theme={theme}>
    <>
      <GridOverlay show={gridOverlay} />
       <LeadStory1 {...{ leadArticle,articles,smallArticles,groupedArticles,singleArticle,articlesWithListItems, clickHandler, expirableFlag, contentType, articleLabel}} />
    </>
  </TCThemeProvider>
);
LeadStory1Story.args = {
  theme: 'TimesWebLightTheme',
};

<Story 
  name="LeadStory1"
  args={{
    leadArticle,
    articles,
    smallArticles,
    groupedArticles,
    singleArticle,
    articlesWithListItems,
    clickHandler: action("ClickHandler"),
    expirableFlag:"BREAKING",
    contentType:"Q&A",
    articleLabel:"MARTIN SAMUAL",
  }}
  argTypes={{
    theme: {
      options: Object.keys(themes),
      mapping: themes,
      control: { type: 'select' },
    },
    gridOverlay: {
      control: { type: 'boolean' },
    },
  }}
>
  {LeadStory1Story.bind({})}
</Story>