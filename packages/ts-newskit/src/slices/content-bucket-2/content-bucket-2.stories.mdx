import { Meta, Story, Props } from '@storybook/addon-docs'
import { TCThemeProvider, themes, GridOverlay } from '../../utils/index.tsx';
import { ContentBucket2 } from "./";
import { section, articles } from '../fixtures/data.json';

<Meta
  title="Newskit Slices/Content Bucket 2"
  component={ContentBucket2}
/>

# ContentBucket2 component
The `ContentBucket2` component is used to display the main story, supplementary articles and journalist quotes.
It is comprised of the `SliceHeader`, and `ArticleListItem` slice components.

This takes in a series of props, as below, to display the part of the slices.

## Props
<Props of={ContentBucket2} />

## Code Example
`<ContentBucket2 {...{ section, articles }} />`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const ContentBucket2Story = ({ section, articles, theme, gridOverlay }) => (
  <TCThemeProvider theme={theme}>
    <>
      <GridOverlay show={gridOverlay} />
      <ContentBucket2 {...{ section, articles }} />
    </>
  </TCThemeProvider>
);
ContentBucket2Story.args = {
  theme: 'TimesWebLightTheme',
};

<Story
  name="ContentBucket2"
  args={{
    section,
    articles
  }}
  argTypes={{
    theme: {
      options: Object.keys(themes),
      mapping: themes,
      control: { type: 'select' },
    },
    gridOverlay: {
      control: { type: 'boolean' },
    },
  }}
>
  {ContentBucket2Story.bind({})}
</Story>