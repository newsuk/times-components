import { Meta, Story, Props } from '@storybook/addon-docs'
import { TCThemeProvider, themes, GridOverlay } from '../../utils/index.tsx';
import { SectionBucket } from "./";
import { articleStackOne, articleStackTwo, articleStackThree, articleStackFour } from '../fixtures/data.json';

<Meta
  title="Newskit Slices/Section Bucket"
  component={SectionBucket}
/>

# SectionBucket component
The `SectionBucket` component is used to display the main story and supplementary articles with a section header.

This takes in a series of props, as below, to display the part of the slices.

## Props
<Props of={SectionBucket} />

## Code Example
`<SectionBucket {...{ articleStackOne, articleStackTwo, articleStackThree, articleStackFour }} />`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const SectionBucketStory = ({ articleStackOne, articleStackTwo, articleStackThree, articleStackFour, theme, gridOverlay }) => (
  <TCThemeProvider theme={theme}>
    <>
      <GridOverlay show={gridOverlay} />
      <SectionBucket {...{ articleStackOne, articleStackTwo, articleStackThree, articleStackFour }} />
    </> 
  </TCThemeProvider>
);
SectionBucketStory.args = {
  theme: 'TimesWebLightTheme',
};

<Story
  name="SectionBucket"
  args={{ articleStackOne, articleStackTwo, articleStackThree, articleStackFour }}
  argTypes={{
    theme: {
      options: Object.keys(themes),
      mapping: themes,
      control: { type: 'select' },
    },
  gridOverlay: {
     control: { type: 'boolean' },
    },
  }}
>
  {SectionBucketStory.bind({})}
</Story>