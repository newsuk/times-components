export const pageStylePresets = {
  pageContainerSmall: {
    base: {
      maxWidth: '520px',
      opacity: '1' // breaks build if removed !!
    }
  },
  pageContainerMedium: {
    base: {
      maxWidth: '820px',
      opacity: '1' // breaks build if removed !!
    }
  },
  pageContainerLarge: {
    base: {
      maxWidth: '1024px',
      opacity: '1' // breaks build if removed !!
    }
  },
  pageContainerExtraLarge: {
    base: {
      maxWidth: '1180px',
      opacity: '1' // breaks build if removed !!
    }
  }
};
