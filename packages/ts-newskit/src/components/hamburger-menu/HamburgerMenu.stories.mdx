import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { Menu } from 'newskit';
import HamburgerMenu from "./HamburgerMenu.tsx";
import SearchBar from './SearchBar.tsx';
import { boolean, object } from "@storybook/addon-knobs"
import { TimesWebLightTheme } from '../../theme';
import { ThemeProvider } from 'newskit';
import NavButtonSection, { LoggedInNavButtons, LoggedOutNavButtons } from './NavButtons.tsx'
import LoggedInMenuItem from './LoggedInMenuItem.tsx';
import LoggedOutNavButton from './LoggedOutNavButton';
import NavItems from './NavItems.tsx';
import data from './fixtures/data.json';


<Meta
  title="Newskit/HamburgerMenu"
  component={HamburgerMenu}
/>

# Markdown HamburgerMenu component

Component's description and any notes worth mentioning

<Canvas isColumn withSource="open" withToolbar>
  <Story name="HamburgerMenu Logged in">
    <HamburgerMenu data={data} loggedIn={true} navbarVisibility={true} />
  </Story>
  <Story name="HamburgerMenu Logged out">
      <HamburgerMenu data={data} loggedIn={false} navbarVisibility={true}/>
  </Story>
  <Story name="Search Bar">
    <ThemeProvider theme={TimesWebLightTheme}>
      <SearchBar />
    </ThemeProvider>
  </Story>
   <Story name="Nav Button Section">
    <ThemeProvider theme={TimesWebLightTheme}> 
      <NavButtonSection loggedIn={boolean('Is logged in', true)} handleClickMain={() => console.log('Handle Click Main')} handleClickAccount={() => console.log('Handle Click Account')} />
        </ThemeProvider>
  </Story>
  <Story name="Logged In menu item">
    <ThemeProvider theme={TimesWebLightTheme}>
    <LoggedInMenuItem title="Section" handleClick={() => console.log('Clicked')} />
    </ThemeProvider>
  </Story>
  <Story name="Logged Out Button Primary">
    <ThemeProvider theme={TimesWebLightTheme}>
    <LoggedOutNavButton title="Subscribe" preset="buttonSolidPrimary" />
    </ThemeProvider>
  </Story>
  <Story name="Logged Out Button Secondary">
    <ThemeProvider theme={TimesWebLightTheme}>
    <LoggedOutNavButton title="Log in" preset="buttonSolidSecondary" />
    </ThemeProvider>
  </Story>
</Canvas>

