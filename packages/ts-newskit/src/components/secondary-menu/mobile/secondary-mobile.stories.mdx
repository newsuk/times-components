import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { SecondaryNavMobile } from "./index.tsx";
import { NavItems } from './navItems';
import { ThemeProvider } from 'newskit';
import { TimesWebLightTheme } from '../../../theme';
import { Menu } from 'newskit';
import { Navigator } from './navigator';
import { mainMenuItems } from '../fixtures/menu-items.json';

<Meta
  title="Newskit/Secondary Menu Mobile"
  component={SecondaryNavMobile}
/>

# Secondary Navigation component

Secondary Navigation Mobile
 
<Canvas>
  <Story name="Secondary Menu Mobile">
      <SecondaryNavMobile data={mainMenuItems} title="Top Stories" isActive={true}/>
  </Story>
</Canvas>

NavItems

<Canvas>
  <Story name="Secondary Menu Items">
    <ThemeProvider theme={TimesWebLightTheme}>
      <Menu vertical>
         <NavItems data={mainMenuItems} />
      </Menu>
    </ThemeProvider>
  </Story>
</Canvas>

Text Block and Dropdown

<Canvas>
  <Story name="Secondary Navigator">
    <ThemeProvider theme={TimesWebLightTheme}>
      <Menu vertical>
         <Navigator title="Top Stories" subMenuTitle="Close"/>
      </Menu>
    </ThemeProvider>
  </Story>
</Canvas>

