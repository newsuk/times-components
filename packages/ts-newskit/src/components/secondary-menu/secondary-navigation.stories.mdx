import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { NewsKitProvider } from 'newskit';
import { TimesWebLightTheme } from '../../theme';
import { SecondaryNavigation } from './index.tsx';
import { mainMenuItems } from '../fixtures/data.json';
import { INITIAL_VIEWPORTS } from '@storybook/addon-viewport';

<Meta
  title="Newskit/Secondary Navigation"
  component={SecondaryNavigation}
  parameters={{
    layout: 'fullscreen',
    viewport: {
      viewports: INITIAL_VIEWPORTS,
      defaultViewport: 'md',
    },
  }}
/>

# Secondary Navigation component

Secondary Navigation

<Canvas>
  <Story name="Secondary Navigation">
    <NewsKitProvider theme={TimesWebLightTheme}>
         <SecondaryNavigation data={mainMenuItems[1].items}/>
    </NewsKitProvider>
  </Story>
</Canvas>