import { Meta, Story, Canvas ,Props} from '@storybook/addon-docs'
import { NewsKitProvider } from 'newskit';
import { TimesWebLightTheme } from '../../theme';
import { SecondaryNavigation } from './index.tsx';
import { mainMenuItems } from './fixtures/menu-items.json';
import { TCThemeProvider } from '../utils/index.tsx';

<Meta
  title="Newskit/Secondary Navigation"
  component={SecondaryNavigation}
/>


# Secondary Navigation component

Secondary Navigation component takes only data as a prop.
## Props
<Props of={SecondaryNavigation} />

## Code Example
`<SecondaryNavigation data={mainMenuItems}/>`

An example of the `data` structure can be found in the 'Controls' section on the 'Canvas' tab, where you can customise the data being fed to the Secondary Navigation component.

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const SecondaryNavStory = ({data}) => (
  <TCThemeProvider>
    <SecondaryNavigation data={mainMenuItems} />
  </TCThemeProvider>
);

<Story name="Secondary Navigation" args={{ data: mainMenuItems }}>
  {SecondaryNavStory.bind({})}
</Story>

