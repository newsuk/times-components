import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { SectionHeader } from "./index";
import { text } from '@storybook/addon-knobs';

import { ThemeProvider } from 'newskit';
import { TimesWebLightTheme } from '../../theme';

<Meta title="Newskit/Section Header" component={SectionHeader} />

# Section Title component
Simple Framework for displaying section titles, the fixed test is to show that special characters are rendered properly.

<Canvas>
  <Story name="Section Header">
    <ThemeProvider theme={TimesWebLightTheme}>
      <SectionHeader title={text('Title', 'News')} />
    </ThemeProvider>
  </Story>
</Canvas>

<Canvas>
  <Story name="Fixed Section Header">
    <ThemeProvider theme={TimesWebLightTheme}>
      <SectionHeader title={'Bricks & Mortar'} />
    </ThemeProvider>
  </Story>
</Canvas>