import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { ThemeProvider } from 'newskit';
import { TimesWebLightTheme } from '../../theme';
import { SectionHeaderTitle } from "./index";
import { sectionTitles } from './fixtures/sectiontitle-items.json';

import { text } from '@storybook/addon-knobs';
const content = text('Text', 'News');

<Meta title="Newskit/Section Header" component={SectionHeaderTitle} />

# Section Title component
Simple Framework for displaying a section title.

<Canvas>
  <Story name="Section Title">
    <ThemeProvider theme={TimesWebLightTheme}>
      <SectionHeaderTitle>
        <TextBlock
          as="span"
          typographyPreset="sectionTitle010"
        >
          {content}
        </TextBlock>
      </SectionHeaderTitle>
    </ThemeProvider>
  </Story>
</Canvas>