import { SecondaryMenuOptions } from '../types';

export const options: SecondaryMenuOptions = {
  isSelected: 'News',
  handleSelect: () => {
    // noop
  },
  isExpanded: true,
  setIsExpanded: () => {
    // noop
  }
};
