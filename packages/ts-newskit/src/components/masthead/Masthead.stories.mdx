import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { EditionMasthead } from "./index";

import { boolean } from "@storybook/addon-knobs"

<Meta title="Newskit/Masthead" component={EditionMasthead} />

# Masthead component
Needs work, but this is the basic framework required for the masthead.

<Canvas>
  <Story name="The Times">
      <EditionMasthead isSunday={boolean('Is it sunday?', false)} />
  </Story>
</Canvas>

<Canvas>
  <Story name="The Sunday Times">
      <EditionMasthead isSunday={boolean('Is it sunday?', true)} />
  </Story>
</Canvas>
