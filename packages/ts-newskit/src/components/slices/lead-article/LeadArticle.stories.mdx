import { Meta, Story, Props } from '@storybook/addon-docs'
import { LeadArticle } from "./index";
import { NewsKitProvider } from 'newskit';
import { TimesWebLightTheme } from '../../../theme/times-web-light/index';
import { leadArticle as leadArticleData } from '../../../slices/fixtures/data.json';

<Meta
  title="Newskit Components/Slices/LeadArticle"
  component={LeadArticle}
/>

# LeadArticle component
The `LeadArticle` component is used to display the main story of slices.

This takes in a series of props, as below, to display the part of the slices.

## Props
<Props of={LeadArticle} />

## Code Example
`<LeadArticle data={leadArticleData}/>`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const LeadArticleStory = ({headline, color, flag, standfirst, bylines, tagL1, caption, images, url, tag, imageTop}) => (
  <NewsKitProvider theme={TimesWebLightTheme}>
    <LeadArticle {...{headline, color, flag, standfirst, bylines, tagL1, caption, images, url, tag, imageTop}} />
  </NewsKitProvider>
);

<Story name="LeadArticle" args={{...leadArticleData}}>
  {LeadArticleStory.bind({})}
</Story>