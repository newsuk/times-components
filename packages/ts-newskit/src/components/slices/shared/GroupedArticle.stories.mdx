
import { Meta, Story, Props } from '@storybook/addon-docs'
import { GroupedArticle } from "./grouped-article";
import { NewsKitProvider } from 'newskit';
import { TimesWebLightTheme } from '../../../theme/times-web-light/index';

<Meta
  title="Newskit Components/Slices/Grouped Article"
  component={GroupedArticle}
/>

# GroupedArticle component
The `GroupedArticle` component is used to display the main story of slices.

This takes in a series of props, as below, to display the part of the slices.

## Props
<Props of={GroupedArticle} />

## Code Example
`<GroupedArticle data={groupedArticle}/>`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const GroupedArticleStory = ({tagL1 , articles}) => (
  <NewsKitProvider theme={TimesWebLightTheme}>
    <GroupedArticle {...{ tagL1 , articles}} />
  </NewsKitProvider>
);

<Story name="GroupedArticle" args={{ 
  tagL1:{
    label:"TAG",
    href:"/"
  }, 
  articles:[
  {
    "headline": "Short title of the card describing the main content",
    "standfirst": "Short paragraph description of the article, outlining main story and focus.",
    "url": "https://www.thetimes.co.uk",
  },
  {
    "headline": "Short title of the card describing the main content",
    "standfirst": "Short paragraph description of the article, outlining main story and focus.",
    "url": "https://www.thetimes.co.uk",
  }
]}}>
  {GroupedArticleStory.bind({})}
</Story>