import { Meta, Story, Canvas } from '@storybook/addon-docs'
import { boolean } from "@storybook/addon-knobs"
import { NewsKitProvider } from 'newskit';
import { TimesWebLightTheme } from '../../../theme';
import { TopNav } from "./topnav.tsx";
import data from '../../fixtures/data.json';

<Meta
  title="Newskit/TopNavigation"
  component={TopNav}
/>

# Markdown Top Navigation component
Only works at XS, SM, LG and XL viewports. Change the viewport above to see the content.

<Canvas>
  <Story name="TopNav">
    <NewsKitProvider theme={TimesWebLightTheme}>
      <TopNav mainMenu={data.mainMenuItems} accountMenu={data.accountMenuItems} isLoggedIn={boolean('Is logged in', false)} />
    </NewsKitProvider>
  </Story>
</Canvas>


