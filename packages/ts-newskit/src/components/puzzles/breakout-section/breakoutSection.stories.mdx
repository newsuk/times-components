import { Meta, Story, Props } from '@storybook/addon-docs'
import { TCThemeProvider } from '../../../utils/index.tsx';
import { PuzzlesWebLightTheme } from '../../../theme';
import { BreakoutSection } from './index';

<Meta
  title="Newskit Components/Puzzles/BreakoutSection"
  component={BreakoutSection}
/>

# BreakoutSection component
The `BreakoutSection` component is used to display a list of available challenges.

## Props
<Props of={BreakoutSection} />

## Code Example
`<BreakoutSection />
`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const BreakoutSectionStory = ({ data }) => (
  <TCThemeProvider theme={PuzzlesWebLightTheme}>
    <BreakoutSection {...{data}} />
  </TCThemeProvider>
);

<Story name="BreakoutSection">
  {BreakoutSectionStory.bind({})}
</Story>