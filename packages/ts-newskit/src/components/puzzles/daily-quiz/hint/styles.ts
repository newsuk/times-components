import { styled, TextBlock } from 'newskit';

export const StyledTextBlock = styled(TextBlock)`
  text-align: center;
`;

export const HintLink = styled(TextBlock)`
  cursor: pointer;
  text-decoration: none;
`;
