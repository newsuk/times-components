import { Meta, Story, Props } from '@storybook/addon-docs'
import { TCThemeProvider } from '../../../utils/index.tsx';
import { PuzzlesWebLightTheme } from '../../../theme';
import { ContactUs } from './index';
import { contactItems } from './fixtures/data.json';

<Meta
  title="Newskit Components/Puzzles/ContactUs"
  component={ContactUs}
/>

# ContactUs component
The `ContactUs` component is used to display contact section.

## Props
<Props of={ContactUs} />

## Code Example
`<ContactUs data={contactItems} header='Need more help?'/>
`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const ContactUsStory = ({ data, header }) => (
  <TCThemeProvider theme={PuzzlesWebLightTheme}>
    <ContactUs data={data} header={header}/>
  </TCThemeProvider>
);

<Story name="Contact Us" args={{
  data: contactItems,
  header: 'Need more help?'
}}>
  {ContactUsStory.bind()}
</Story>

