import { Meta, Story, Props } from '@storybook/addon-docs'
import { NewsKitProvider } from 'newskit';
import { PuzzlesWebLightTheme } from '../../../theme';
import { OffersBanner } from './index';

<Meta
  title="Newskit Components/Puzzles/OffersBanner"
  component={OffersBanner}
/>

# OffersBanner component
The `OffersBanner` component is used to display a button which leads to the offers page

## Props
<Props of={OffersBanner} />

## Code Example
`<OffersBanner />
`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const OffersBannerStory = () => (
  <NewsKitProvider theme={PuzzlesWebLightTheme}>
    <OffersBanner />
  </NewsKitProvider>
);

<Story name="OffersBanner">
  {OffersBannerStory.bind({})}
</Story>
