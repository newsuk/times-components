import { Meta, Story, Props } from '@storybook/addon-docs'
import { Archive } from "./index";
import { NewsKitProvider, Block } from 'newskit';
import { TimesWebLightTheme } from '../../../theme';
import { sudokuArchive } from './fixtures/sudokuArchive.json';
import { corsswordsArchive } from './fixtures/corsswordsArchive.json';
import { wordPuzzlesArchive } from './fixtures/wordPuzzlesArchive.json';
import { numbersAndLogicArchive } from './fixtures/numbersAndLogicArchive.json';
import { quizzesAndTeasersArchive } from './fixtures/quizzesAndTeasersArchive.json';
import { boardAndCardArchive } from './fixtures/boardAndCardArchive.json';

<Meta
  title="Newskit Components/Puzzles/Archive"
  component={Archive}
/>

# Archive component
The `Archive` component is used to display a list of archived Puzzle games, filtered by title inside the NewsKit Accordions.

## Props 
<Props of={Archive} />

## Code Example
`<Archive data={sudokuArchive}/>`

## View Component
Please click the 'Canvas' tab for a better viewing experience, where you can update the props and review at the different breakpoints by clicking the preview icon and selecting from our list of pre-defined breakpoints (XS, SM, MD, LG and XL).

export const ArchiveStory = ({ data, seeMoreLink }) => (
  <NewsKitProvider theme={TimesWebLightTheme}>
    <Block marginInline='space030' marginBlock='space020'>
      <Archive data={data} seeMoreLink={seeMoreLink}/>
    </Block>
  </NewsKitProvider>
);

<Story name="Archive" args={{data: sudokuArchive, seeMoreLink: 'https://newskit.co.uk/'}} argTypes={{
  data: {
    control: 'select', 
    options: ['Sudoku', 'Crosswords', 'WordPuzzles', 'NumbersAndLogic', 'QuizzesAndTeasers', 'BoardAndCard'],
    mapping: { 
      Sudoku: sudokuArchive,
      Crosswords: corsswordsArchive,
      WordPuzzles: wordPuzzlesArchive,
      NumbersAndLogic: numbersAndLogicArchive,
      QuizzesAndTeasers: quizzesAndTeasersArchive,
      BoardAndCard: boardAndCardArchive            
    },
  },
  seeMoreLink: { control: 'text'}
}}>
  {ArchiveStory.bind({})}
</Story>
