import { createContext } from "react";

export default createContext({
  // TODO: Clean up
  publicationName: "TIMES",
  readArticleStore: {},
  recentlyOpenedPuzzles: 0,
  savedArticleStore: {},
});
