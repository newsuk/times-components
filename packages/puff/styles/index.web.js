import { StyleSheet } from "react-native";
import styles from "./index.base";

export default StyleSheet.create({
  ...styles,
  textContainer: {
    padding: 10,
    paddingTop: 5
  },
  withoutImage: {
    paddingTop: 10
  }
});
