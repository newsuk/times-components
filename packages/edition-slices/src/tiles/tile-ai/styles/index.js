import { spacing } from "@times-components/styleguide";

const styles = {
  container: {
    alignItems: "center",
    paddingBottom: 16,
    paddingTop: spacing(2)
    // width: "100%"
  },
  imageContainer: {
    width: "100%"
  }
};

export default styles;
