import { spacing } from "@times-components/styleguide";

const styles = {
  container: {
    alignItems: "center",
    paddingBottom: spacing(3)
  },
  imageContainer: {
    width: "100%"
  },
  starButton: {
    textAlign: "center",
    marginTop: 10
  }
};

export default styles;
