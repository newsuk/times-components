import { spacing } from "@times-components/styleguide";

const styles = {
  container: {
    alignItems: "center",
    paddingVertical: spacing(2)
  },
  imageContainer: {
    width: "100%"
  }
};

export default styles;
