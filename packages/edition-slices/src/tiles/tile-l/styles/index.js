import { fonts } from "@times-components/styleguide";

const styles = {
  headlineStyle: {
    fontFamily: fonts.headline,
    fontSize: 22,
    lineHeight: 22,
    marginBottom: 11,
    marginTop: 5
  }
};

export default styles;
