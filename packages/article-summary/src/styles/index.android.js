import sharedStyles from "./shared";

const styles = {
  ...sharedStyles,
  labelWrapper: {
    ...sharedStyles.labelWrapper
  },
  headline: {
    ...sharedStyles.headline,
    fontWeight: "400"
  }
};

export default styles;
