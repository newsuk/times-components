import sharedStyles from "./shared";

const styles = {
  ...sharedStyles,
  labelWrapper: {
    ...sharedStyles.labelWrapper,
    marginBottom: 5
  },
  default: {
    ...sharedStyles.default,
    fontSize: 22,
    lineHeight: 22
  }
};

export default styles;
